<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:centerColor="@color/main_divider"
        android:endColor="@color/main_divider"
        android:startColor="@color/main_divider"
        android:type="linear" />
    <size
        android:width="0.4dp"
        android:height="0.4dp" />

</shape>